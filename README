# barebones plugin framework

## plugins subclass plugplug.plugin.Plugin
* must have:
 * inputs(*args,**kwargs)
 * outputs(*args,**kwargs)
 * errors(*args,**kwargs)

## to use plugins
<code>
import plugplug

example = plugplug.load('example') # to load example.py

example.inputs('hello world')

example.outputs()
> 'hello world'
# or
example.outputs('caps')
> 'HELLO WORLD'
# or
example.outputs(caps=True)
> 'HELLO WORLD'
</code>
